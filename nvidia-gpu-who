nvidia-gpu-pid | grep "#" | awk -F ":" '{ print "GPU:" $1 ", process_id: " $2 ", Memory used (in MB): " $4 ", docker_id: " $3}{if (!match($3, /docker/)) { system("docker inspect " $3 "| egrep \"dgxdata|home\"") } }'
